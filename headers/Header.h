/*
	Header Template
	COMP 345 Project
	@author: Kevin McAllister (40031326)
*/
// Header Guard (Visual Studio also has #pragma once, but these are the more standard header guards that make the code useable outside of the visual studio compiler)
#ifndef HEADER_H
#define HEADER_H
// Includes

// Namespaces

// 3 Main types of headers: Class, Namespace, Function (headers can have multiple in each, but can become cluttered)

#endif // !HEADER_H